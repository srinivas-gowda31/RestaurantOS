# Leave blank for local dev - falls back to '/api', which vite.config.js proxies to localhost:5000.
# Set to the deployed backend's URL (including /api) only for a production build.
VITE_API_BASE_URL=
